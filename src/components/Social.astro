---
const { platform, username } = Astro.props;
// const platformLowerCase = platform.toString().toLowerCase()
---

<a href={`https://www.${platform.toLowerCase()}.com/${username}`}>{platform}</a>

<style>
    a {
      padding: 0.5rem 1rem;
      color: #fff;
      background-color: #4c1d95;
      text-decoration: none;
      border-radius: 10px;
    }
  </style>